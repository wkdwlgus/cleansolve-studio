[project]
name = "cleansolve-studio"
version = "0.1.0"
description = "Math handwriting cleanup editor scaffold"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "langgraph",
  "openai",
  "Pillow",
  "pydantic",
  "pytest",
  "python-multipart",
]
