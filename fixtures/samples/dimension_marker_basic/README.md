# 기본 치수 표시 Fixture

이 fixture는 라벨이 붙은 끊어진 자유곡선 치수 표시를 나타냅니다. `freehand_dimension_marker`가 단순한 `highlight_curve`로 해석되지 않도록 두 타입의 구분을 보호합니다.

현재는 원본 이미지를 커밋하지 않고 JSON 계약만 사용합니다. 초기 scaffold harness에서는 이 JSON 계약만으로 mock 분석 결과, 렌더링 제약, 보정 계획, 최종 상태를 검증합니다.
